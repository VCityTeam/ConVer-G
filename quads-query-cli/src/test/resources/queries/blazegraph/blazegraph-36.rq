# Follow the specializationOf chain from every versioned named graph (metadata)
PREFIX prov: <http://www.w3.org/ns/prov#>

SELECT ?vng ?ng WHERE {
  GRAPH <https://github.com/VCityTeam/ConVer-G/Named-Graph#Metadata> {
    ?vng prov:specializationOf+ ?ng .
  }
}
